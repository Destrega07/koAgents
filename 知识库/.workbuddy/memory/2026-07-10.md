# 2026-07-10 工作日志

## 执行指令 d：证据卡信号强度校准（舆情合集两份）

按 `执行指令d_舆情报告证据卡校正.md` + `证据卡信号强度校准规范.md`，对上轮两份证据卡做贝叶斯信号强度校准，原文件就地修改。

### 交付物（原地改写）
- `证据卡\Customer\证据卡_海底捞舆情合集260609-0708.md`
- `证据卡\Customer\证据卡_汉堡王舆情合集260609-0708.md`

### 结构（每文件）
- Part A 修订原子卡（新增 factual_strength / strategic_weight / max_update_scope / weight_rationale 四字段，bayes 引入 4 档）
- Part B 模式卡（pattern_card + related_evidence_cards，单卡 weak→模式卡 medium）
- Part C 修订说明表（原/新 bayes、新 strategic_weight、降级升级、原因）
- Part D 排除清单（沿用上轮，未改）

### 校准结果
- 海底捞：原子卡 strong 2 / medium 10 / weak 14（原 strong 19→仅保留 10 财报、18 董事会致歉）；模式卡 4。
- 汉堡王：原子卡 strong 4 / medium 5 / weak 21（原 strong 21→保留 01 加盟冲突、02 网信办、14 两年关店、18 普宁查处）；模式卡 5。
- 核心原则落地：单店事故封顶 weak、投诉平台限 weak/medium、监管/财报/官方保留 strong、重复信号聚合为模式卡。

### 澄清决策（用户拍板）
- 字段名拼写：采用标准拼写 `pattern_card` / `related_evidence_cards`（指令 d 原文 typo 为 patten_card / related_evidance_cards，已按用户指定修正）。
- 原排除清单：保留为 Part D 附录，不删除。

### 校验
- Python yaml.safe_load 100% 通过；无重复 id；模式卡均含 related_evidence_cards。

## 沉淀可复用 Skill：opinion-evidence-card（项目级）

用户要求把"从舆情合集提取证据卡"的经验提炼为 Skill。已创建项目级 Skill：
- 路径：`D:\AI_Projects\koAgents\知识库\.workbuddy\skills\opinion-evidence-card\`（SKILL.md + references/calibration_reference.md）
- 内容：两阶段流程（拆卡事实筛选 → 4档贝叶斯校准）、字段 schema、id 规则、校准矩阵、封顶规则 A/B/C、模式卡规则、10 个避免错误、已批准范例（strong/weak/模式卡三例）。
- 指向项目内权威规范（`执行指令b`、`证据卡信号强度校准规范`、`执行指令d`、汉堡王月度扫描参考卡），保持 DRY。
- 用 skill-creator 的 init_skill.py 脚手架生成，package_skill.py 校验通过。

## 红火先知报告 HTML → Markdown 转换（1:1 同名同路径）

用户要求把 `红火先知报告\` 下所有 HTML 转 Markdown：一个 HTML 对应一个 MD，同名同路径；代码提取 + 大模型重构，保留所有编号(O01/T05/Ixx/Rxx/Vxx/EC-x)、标签(高/中/高概率/高置信等)、事实数据。

### 方法
- 托管 Python 建 venv 装 beautifulsoup4（`C:\Users\destr\.workbuddy\binaries\python\envs\default`）。
- 写类感知提取器 `D:/tmp/extract_html_reports.py`：处理 .report-header/.summary-text/.section/.section-header/.id-tag/.tag/.field-row/.change-item(.change-icon+title+detail+implication)/reasoning-card/opp-card/verify-item/cluster-card 等，输出草稿 MD。
- 大模型重构：识别并修复代码提取的格式 glitch（`.change-icon` A-E 变成孤立单行），核对编号/标签/数据零丢失。

### 交付物（红火先知报告\，与 HTML 同名同路径）
- `汉堡王中国洞察报告_20260611.md`（465 行）
- `海底捞集团洞察报告_20260611.md`（465 行）
- 编号完整性校验：汉堡王 EC-A~G/I01-08/O01-04/R01-02/T01-05/V01-05；海底捞 EC-A~H/I×16/O×8/R×4/T×5/V×6/变化项 A-F —— 与 HTML 一致。
- 关键事实保留：40万美元 AUV、SSSG+10.5%、12.9元、3.5亿美元、4000家、张勇、红石榴、翻台率等均在终稿。
